#!/usr/bin/env bash

if ! [[ -z "$1" ]]; then
    base="$1"
else
    base="$(echo -e ""$HOME/.dev"\n"$HOME"/.config\n"$HOME"/.dotfiles" | fzf)" 
fi

fzf --walker=dir,hidden \
    --walker-root="$base" \
    --walker-skip=.git,.venv,.stfolder
