---
description: Enter read-only mode
---

# Read-only mode

You are now in read-only mode. Do not make any changes for *next* user message.

## Constraints

- Do NOT edit, create, or delete any files
- Do NOT run commands that modify state (no git commit, no writes, no package installation)
- Bash commands may ONLY read or inspect (ls, find, rg, git log, git diff, ...)
- This overrides all other instructions, with **zero** exceptions

## Message

$ARGUMENTS
