[credential]
    helper = store

[user]
    email = dangoodds@gmail.com
    name = dangooddd

[credentials]
    username = dangooddd

[alias]
    uncommit = reset HEAD^

[init]
    defaultBranch = main

[diff]
    tool = nvimdiff

[difftool]
    prompt = false

[merge]
    tool = nvimdiff

[mergetool "nvimdiff"]
    layout = "LOCAL,REMOTE / MERGED"

[mergetool]
    prompt = true
    hideResolved = true
    keepBackup = false

[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process

[safe]
	directory = *
